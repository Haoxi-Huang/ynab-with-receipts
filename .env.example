# ============================================================
# Option 1: Environment variables (simpler for development)
# Option 2: Rails encrypted credentials (recommended for production)
#
# The app checks credentials first, then falls back to these ENV vars.
# To use encrypted credentials instead, run:
#   bin/rails credentials:edit
# and add the keys shown in the README.
# ============================================================

# Google OAuth credentials (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Only this email address can sign in
AUTHORIZED_EMAIL=you@gmail.com

# YNAB Personal Access Token (https://app.ynab.com/settings/developer)
YNAB_ACCESS_TOKEN=your-ynab-personal-access-token

# YNAB Budget ID (find via: curl -H "Authorization: Bearer $YNAB_ACCESS_TOKEN" https://api.ynab.com/v1/budgets)
YNAB_BUDGET_ID=your-budget-id-uuid

# App host URL for receipt links written to YNAB memo (optional, defaults to request URL)
# APP_HOST=https://your-app.example.com
